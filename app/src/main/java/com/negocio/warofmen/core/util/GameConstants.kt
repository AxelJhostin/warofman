package com.negocio.warofmen.core.util

object GameConstants {
    // Configuración de Niveles
    const val XP_MULTIPLIER = 1.2f        // Cuánto sube la dificultad por nivel (20%)
    const val BASE_XP_REQ = 100           // XP necesaria para nivel 2

    // Configuración de Stats
    const val BASE_STAT_VALUE = 5         // Valor inicial de atributos
    const val STAT_POINTS_PER_LEVEL = 1   // Cuántos puntos ganas al subir nivel (automático)

    // Recompensas Base
    const val BASE_QUEST_XP = 20          // XP base por misión

    // Tiempos
    const val DEFAULT_TIMER_SECONDS = 10  // Tiempo base para planchas/wall-sit
}